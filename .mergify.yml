pull_request_rules:
  - name: BB review-request team permission boundary merge
    conditions:
      - title = BB review request team gate canary
      - review-requested = @bb-review-request-team-20260815-a1
    actions:
      merge:
        method: merge
